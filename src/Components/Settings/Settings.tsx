import React from 'react';
import styles from './Settings.module.css'
const Settings = () => {
    return (
        <div>
            And I'm Settings-page
        </div>
    );
};

export default Settings;